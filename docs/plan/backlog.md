# Backlog

Ideas and stories for future versions. Reviewed when planning the next version.

## Unversioned

- Jenkins/Forgejo integration (waiting for homelab infrastructure to settle)
- Admin visibility of private projects (policy decision — currently admins cannot see private projects)
- Cross-project task dependencies (currently blocked-by is within the same project only)
- MCP tool: `delete_task` (agents generally should not delete tasks; left for future consideration)
- MCP over HTTP+SSE transport (v1.3 uses stdio; HTTP+SSE enables remote/multi-client scenarios)
